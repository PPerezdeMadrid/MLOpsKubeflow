name: modeloscoring
description: Modelo Scoring
inputs:
- {name: df, type: DataFrame, description: 'DataFrame de entrada'}
- {name: mes, type: Integer, description: 'Mes de los datos'}
outputs:
- {name: output, type: DataFrame, description: 'DataFrame de predicciones'}
implementation:
  container:
    image: <IMAGE_NAME>
    command: [
      python3, /pipeline/modeloscoring.py
    ]
